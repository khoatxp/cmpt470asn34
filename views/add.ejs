<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Add Rectangle</title>
  <link rel="stylesheet" href="/stylesheets/colorinput.css">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
</head>


<body class="container" style="margin-top: 50px;">
    <% if(messages.error ) { %>
        <div class="alert alert-danger" role="danger">
            <ul>
                <% messages.error.forEach(function(msg) { %>
                    <li> <strong> <%= msg %> </strong></li>
                <% }) %>
            </ul>
        </div>
    <% } %> 

<div class="card"> 
    <div class="card-header">
        Add a Rectangle
    </div>
<div class="card-body">
  <form action="/add" method="post">
    <div class="form-group">
        <label for="Width">Width</label>
        <input class="form-control" name="width" value="<%= rows.width %>" placeholder="Size"/>
    </div>
    <div class="form-group">
        <label for="Height">Height</label>
        <input class="form-control" name="height" value="<%= rows.height %>" placeholder="Height"/>
    </div>
    <div class="form-group">
        <label for="Color">Color</label>
        <input type="color" class="form-control" name="color" value="<%= rows.color %>" placeholder="Color"/>
    </div>
    <div class="form-group">
        <input type="submit" value="Add" class="btn btn-sm btn-success"/>
        <a class="btn btn-sm btn-danger" href="/">Cancel</a>
    </div>
    
  </form>
</div>
</div>
</body>
</html>
